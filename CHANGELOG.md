# 更新日志

本文件是发布说明的唯一来源。打标签发布时，`.github/workflows/release.yml`
会按标签名抽出对应小节，作为 GitHub Release 的正文。

写法约定：只写这个版本**真正做了什么**、以及**为什么这么做**；不写没做的事，
不写"优化了性能"这类无法核对的空话。破坏性变更与需要人工介入的事项排在最前。

## v0.1.1

界面改进为主，没有接口变更，也不需要任何迁移或人工介入。

### 在线更新

- **从设置页移到左侧导航，成为独立页面。** 版面只回答三个问题：现在跑的是哪一版、
  有没有新的、新的改了什么。更新源仓库不再显示 —— 它是部署时定死的配置，
  看的人既改不了也不需要知道。
- **安装过程改为带步骤动画的弹窗。** 只画两步，因为后端真实只有两段可观测的过程：
  一次请求里完成下载、校验与替换，之后是重启与探活。
- **不显示下载百分比。** 下载发生在服务端，浏览器根本拿不到进度；编一个进度条
  只会让人误判还要等多久。改用不定进度条如实表达"在做，但不知道还要多久"。
- 侧栏底部新增版本号与 GitHub 入口。版本号可点开查看当前这一版的更新内容，
  有新版本时挂一个红点。

### 发布说明的渲染

上一版把发布说明按纯文本显示，`###` 和表格语法直接裸露在界面上。现在改为渲染：

- 支持标题、有序/无序列表、代码块、表格、引用、分隔线，以及行内的加粗、斜体、
  代码、链接与裸 URL 自动成链。
- 列表项的续行会并回上一项，在 GitHub 上换行书写的长条目不再被拆成两条。
- 修正 `---` 分隔线被无序列表规则先匹配、渲染成空列表项的问题。

渲染器只产出 React 元素、全程不使用 `innerHTML`，因此不存在注入风险，
也没有为此引入任何第三方依赖。

### 动效

- 页面切换、看板分块、表格行、统计卡片加入入场动效，统计卡片支持悬浮抬起。
- 全部只使用 `transform` 与 `opacity`，走合成层，不触发重排重绘。
- 系统开启"减少动效"时全部关闭，唯独保留不定进度条 —— 它是"正在进行"的
  唯一提示，停掉会让人误以为卡死。

### 构建

- 发布说明改由仓库中的 `CHANGELOG.md` 驱动，发布时按标签抽取对应小节，
  不再在构建期拼接文案。
- 升级 GitHub Actions 依赖至 Node 24 运行时，消除弃用告警。

## v0.1.0

首个发布。Outlook 账号池的管理与在线取件系统，前端已嵌入二进制，单文件即可运行。

### 核心能力

- **按需在线取件。** 只在进入详情页、调用 API、点击刷新时才连 Outlook，不做轮询。
  邮件全程不落库，取到的永远是此刻邮箱上的真实状态。
- **令牌自动轮换。** 常驻调度器保证每个账号在 refresh_token 的 90 天硬过期前完成轮换。
  速率由积压自动推导，账号规模增长时无需改配置。
- **三条取件通道。** Graph、IMAP、POP3 自动降级。降级到 POP3 会看不到垃圾邮件，
  响应里的 `folder_coverage` 会如实标注本次真正覆盖了哪些文件夹。
- **账号与出口 IP 粘性绑定。** 同一账号始终从同一 IP 出网，支持代理分组、
  故障转移与人工钉死。轮换 IP 本身就是风控信号，因此默认不换。
- **开放 API。** `/api/v1` 提供取件、长轮询等新邮件、验证码提取、账号租约与批量导入。

### 防误判

- 网络故障与限流**绝不**把账号标记为失效，避免微软侧一次抖动批量误杀账号。
- 应用被封时按 `client_id` 熔断，而不是逐个把账号判死。
- `invalid_grant` 下细分 AADSTS 子码，只有明确表示授权码失效的才停用账号；
  scope 未授权这类问题改为标记该通道不可用并降级。

### 部署与运维

- **单文件部署。** 前端经 `go:embed` 打进二进制，服务器上不需要 Nginx 托管静态资源。
- **一键在线更新。** 从 GitHub Releases 拉取新版，校验 SHA256 后替换并自动重启。
  Linux 走 `execve` 原地换映像，不依赖进程守护。
- 提供 linux 与 windows 的 amd64 / arm64 四个平台产物。
- 数据库支持 PostgreSQL 与 SQLite，一套 SQL 两种方言。生产用前者，本地开发用后者。

### 安全

- 授权码与账号密码一律 AES-256-GCM 加密存储，任何列表接口都不返回密文或明文。
- 查看账号密码需用登录密码解锁本次会话，且每次查看都写审计日志。
- 导出含令牌的数据需要二次确认，并且必须限定范围，不允许一次导出全量。
- 安装更新需要重新输入登录密码。

### 已知限制

- 邮件不落库意味着**没有历史**：调用方错过的验证码在系统内找不回，只能重新触发发信。
- 调度器关闭后，闲置账号会在 90 天后失效。总览页会显示第一个账号的预计失效日期。
- Windows 上的自动更新会重启出一个新进程，进程号会变；Linux 上进程号不变。
